# CAT quant core (R0 backtest) — host venv deps.
# Kept minimal and framework-free: no FastAPI/Celery/SQLAlchemy here. quant/ is
# imported into the Python 3.12 backend container in R1, so pin nothing that
# would conflict with the backend image.
pandas>=2.1,<3
pyarrow>=15
numpy>=1.26,<3
alpaca-py>=0.20            # market data + (R1) execution client
exchange-calendars>=4.5    # XNYS trading calendar (design §8.2)
python-dotenv>=1.0         # read repo .env for API keys (never hardcode)
vectorbt>=0.26             # single-symbol signal-quality sweeps (D1)
pytest>=8.0
