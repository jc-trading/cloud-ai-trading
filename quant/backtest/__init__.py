"""Backtest layer: cost model, thin daily portfolio simulator (D1), metrics,
walk-forward split, and bias checks. The simulator stays thin (<500 lines) and
MUST NOT grow into a general framework (design D1)."""
